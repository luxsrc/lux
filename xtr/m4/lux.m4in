# AX_REQUIRE_LUX
# --------------
AC_DEFUN([AX_REQUIRE_LUX],
[
AC_REQUIRE([AC_PROG_CPP])
AC_REQUIRE([AC_PROG_CC_STDC])
AC_C_CONST
AC_C_INLINE
AC_C_RESTRICT
prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@
if which lux; then
	AC_SUBST([LUX], [`which lux`])
else
	AC_SUBST([LUX], [${bindir}/lux])
fi
])# AX_REQUIRE_LUX
